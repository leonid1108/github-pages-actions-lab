# GitHub Pages Actions Lab

Статический сайт для лабораторной работы по автоматическому деплою через GitHub Actions.

После push в ветку `main` workflow публикует содержимое репозитория в ветку `gh-pages`.
Сайт будет доступен по адресу:

```text
https://<ваш_логин>.github.io/<имя_репозитория>/
```
